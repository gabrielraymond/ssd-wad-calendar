import React from "react";
import withPrivateRoute from "../../shared/libs/WithPrivateRoute";

const DashboardPage = () => {
  return <div>dashboard</div>;
};

export default withPrivateRoute(DashboardPage);
